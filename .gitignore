# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist/
build/

# IDE and local tooling
.vscode/
.idea/
*.swp
*.swo
# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/

# Logs
*.log

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Temporary files
*.tmp
*.temp 
# Instructor materials: the passphrase and the plaintext PDFs
.instructor-password
.instructor-build/
.instructor-fixture/

# Playwright
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# LaTeX build products. manual/*.tex and the PDF at the repository root are
# committed; the compiler's intermediates are not.
manual/*.log
manual/*.aux
manual/*.out
manual/*.toc
manual/*.xdv
manual/gravitas-user-manual.pdf
.build-report.json

# Cached GWOSC downloads for tools/build-gw-data.mjs. The processed result
# is committed; the 1.4 MB of source text is not.
.gw-cache/

# Cached MIST downloads for tools/build-stellar-tracks.mjs. The reduced result
# is committed; the 100 MB source grid is not.
.mist-cache/

# Reports an earlier CI step left for tools/docs-facts.mjs to read back.
.facts/

# The printable workbook (tools/build-workbook.mjs). The scientific table
# beside it is committed; the PDF is not, because it stamps the commit it
# was built from and so can never be current inside that commit.
workbooks/*.pdf
