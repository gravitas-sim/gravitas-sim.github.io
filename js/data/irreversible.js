// GENERATED by tools/build-irreversibility-audit.mjs. Do not edit.
//
// Every operation in the engine that destroys information, found by scanning
// js/physics.js rather than by remembering. Regenerate with
// `npm run audit:irreversible`; `--check` fails when this file and the source
// disagree, which is what stops the model page describing a simulation that no
// longer exists.
//
// "Irreversible" here means precisely one thing: running the integrator
// backwards from the state after cannot produce the state before, because the
// information needed is no longer present. It is not a complaint about any of
// them. A sandbox that never merged anything would be a worse sandbox.

/** @typedef {{id: string, kind: string, line: number, detail: object}} Irreversible */

/** @type {ReadonlyArray<Irreversible>} */
export const IRREVERSIBLE = Object.freeze([
  Object.freeze({
    id: 'merge:absorption',
    kind: 'merge',
    line: 661,
    detail: Object.freeze({
      what: 'a body absorbed by a black hole is deleted and its mass, momentum and spin folded into one object; two histories become one state',
    }),
  }),
  Object.freeze({
    id: 'collapse:star-to-hole',
    kind: 'collapse',
    line: 2958,
    detail: Object.freeze({
      what: 'a star past the collapse mass is replaced by a black hole; the star it was is gone, along with its type, temperature and radius',
    }),
  }),
  Object.freeze({
    id: 'cull:planets',
    kind: 'cull',
    line: 3087,
    detail: Object.freeze({
      list: 'planets',
      buffer: 20,
      what: 'planets more than 20 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:stars',
    kind: 'cull',
    line: 3091,
    detail: Object.freeze({
      list: 'stars',
      buffer: 20,
      what: 'stars more than 20 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:gas_giants',
    kind: 'cull',
    line: 3095,
    detail: Object.freeze({
      list: 'gas_giants',
      buffer: 20,
      what: 'gas giants more than 20 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:neutron_stars',
    kind: 'cull',
    line: 3099,
    detail: Object.freeze({
      list: 'neutron_stars',
      buffer: 20,
      what: 'neutron stars more than 20 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:white_dwarfs',
    kind: 'cull',
    line: 3103,
    detail: Object.freeze({
      list: 'white_dwarfs',
      buffer: 20,
      what: 'white dwarfs more than 20 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:galaxies',
    kind: 'cull',
    line: 3107,
    detail: Object.freeze({
      list: 'galaxies',
      buffer: 20,
      what: 'galaxies more than 20 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:bh_list',
    kind: 'cull',
    line: 3111,
    detail: Object.freeze({
      list: 'bh_list',
      buffer: 50,
      what: 'black holes more than 50 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:asteroids',
    kind: 'cull',
    line: 3117,
    detail: Object.freeze({
      list: 'asteroids',
      buffer: 5,
      what: 'asteroids more than 5 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:comets',
    kind: 'cull',
    line: 3121,
    detail: Object.freeze({
      list: 'comets',
      buffer: 20,
      what: 'comets more than 20 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:debris',
    kind: 'cull',
    line: 3125,
    detail: Object.freeze({
      list: 'debris',
      buffer: 3,
      what: 'debris more than 3 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'cull:accretion_disk_particles',
    kind: 'cull',
    line: 3129,
    detail: Object.freeze({
      list: 'accretion_disk_particles',
      buffer: 2,
      what: 'accretion-disk particles more than 2 view-widths out are deleted',
    }),
  }),
  Object.freeze({
    id: 'damping:orbit_decay',
    kind: 'damping',
    line: 5005,
    detail: Object.freeze({
      what: 'each velocity component is multiplied by (1 - orbit_decay_rate * dt), so energy leaves the orbit and nothing records how much',
    }),
  }),
  Object.freeze({
    id: 'fragment:rocky-collision',
    kind: 'fragment',
    line: 7621,
    detail: Object.freeze({
      draws: 7,
      what: 'a rocky collision destroys both bodies and scatters debris whose directions, speeds and offsets come from 7 fresh Math.random draws - unseeded, so the shower cannot be reproduced, let alone reversed',
    }),
  }),
]);

/** Where the scan looked, so a reader can go and check. */
export const IRREVERSIBLE_SOURCE = 'js/physics.js';

/** How many of each kind, for the documentation counts. */
export const IRREVERSIBLE_KINDS = Object.freeze({
  merge: 1,
  collapse: 1,
  cull: 11,
  damping: 1,
  fragment: 1,
});
